# creating new environment

conda create --name myenv_name
conda activate myenv_name

# creating kernel for new environment

conda install ipykernel
ipython kernel install --user --name=myenv_name-kernel

# rempving any existing kernel 

jupyter kernelspec uninstall yourKernelname

conda deactivate

conda install jupyter


